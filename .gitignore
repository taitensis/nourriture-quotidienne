# ── Build output
dist/
.astro/

# ── TinaCMS (generated)
.tina/__generated__/

# If your host builds the site (Vercel/Netlify/etc.), ignore the generated admin app:
# public/admin/
# If your host does NOT build (e.g., GitHub Pages without CI), do NOT ignore it—commit public/admin.

# ── Dependencies
node_modules/

# ── Environment files
.env
.env.*
!.env.example

# ── Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── OS / Editors
.DS_Store
.idea/
.vscode/             # (optional) ignore your local VS Code settings

# ── Tests (optional; if you add them later)
coverage/

node_modules